import React from 'react';
import { StyleSheet, View } from 'react-native';

const CreateInputModal_ = () => {
    return (
        <View style={styles.root}>
            
        </View>
    );
}

const styles = StyleSheet.create({
    root: {
        flex: 1,
        width: '100%',
        height: '100%',
        backgroundColor: 'dodgerblue',
        position: 'absolute',
        zIndex: 5,
        marginLeft: 'auto',
    }
});

export default CreateInputModal_;